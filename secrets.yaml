# Execut 'git update-index --assume-unchanged ./secrets.yaml' to avoid tracking changes to this file.'
# To revert swap the flag to --no-assume-unchanged

http_password: **password**
ssl_certificate: **ssl_cert**
ssl_key: **ssl_key**

foobot_api_token: **foo_bot_key**

openweathermap_api_key: **owm_key**
